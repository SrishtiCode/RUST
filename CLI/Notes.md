The first thing command line should do is to take two inputs - (i)the file from which it has to search for (ii)the word to search for

So our command should be
`cargo run --searchstring example-filename.txt`
  